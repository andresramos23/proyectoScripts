CREATE TABLE lifeStyle
(
       lifeStyle_id NUMBER(6), CONSTRAINT pk_lifeStyle PRIMARY KEY (lifeStyle_id),
       wantChildren Number(1),
       aboutChildren Number(1),
       likePets Number(1),
       willingPets Number(1),
       hasPets Number(1)
           
);
